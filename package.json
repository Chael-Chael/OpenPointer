{
  "name": "openmagicpointer",
  "version": "0.1.0",
  "private": true,
  "description": "OpenMagicPointer desktop AI pointer assistant.",
  "license": "Apache-2.0",
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "postinstall": "git -C vendor/cua apply ../../patches/cua/0001-get-window-state-elements-structured-output.patch || exit 0",
    "setup:cua": "node scripts/setup-cua.mjs",
    "dev": "npm run dev -w @openmagicpointer/desktop",
    "build": "npm run build -w @openmagicpointer/core && npm run build -w @openmagicpointer/gestures && npm run build -w @openmagicpointer/voice && npm run build -w @openmagicpointer/backends && npm run build -w @openmagicpointer/grounding && npm run build -w @openmagicpointer/storage && npm run build -w @openmagicpointer/agent-bridge && npm run build -w @openmagicpointer/desktop",
    "typecheck": "tsc -b",
    "test": "vitest run",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "9.18.0",
    "@tailwindcss/vite": "^4.3.0",
    "@types/node": "^20.12.12",
    "@vitejs/plugin-react": "^6.0.2",
    "eslint": "9.18.0",
    "eslint-config-prettier": "9.1.0",
    "eslint-plugin-react-hooks": "5.1.0",
    "prettier": "3.4.2",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.4.5",
    "typescript-eslint": "8.21.0",
    "vite": "^8.0.14",
    "vitest": "^4.1.7"
  }
}
